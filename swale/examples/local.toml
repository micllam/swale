# A graph of two shell nodes that runs anywhere `sh` exists:
#
#     swale run examples/local.toml --store ./swale-store
#
# `first` prints an output, and `second` receives it through a template.

[graph]
name = "local"

[[node]]
name = "first"
produces = "first"
operator = "shell"
[node.params]
command = "printf '{\"n\": 1}'"

[[node]]
name = "second"
produces = "second"
consumes = ["first"]
operator = "shell"
[node.params]
command = "printf '{\"n\": {{ upstream.first.n }}}'"
